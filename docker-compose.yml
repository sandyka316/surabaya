version: '2.0'
services:
  main:
    image: main-surabaya
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3005:3005
    env_file:
      - .env.production
    volumes:
      - /media/HDNFS/surabaya/uploads/images:/app/public/images
